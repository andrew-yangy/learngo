apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: learngo-gateway
spec:
  selector:
    istio: ingressgateway # use istio default controller
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: order
spec:
  hosts:
  - "*"
  gateways:
  - learngo-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: order-api
